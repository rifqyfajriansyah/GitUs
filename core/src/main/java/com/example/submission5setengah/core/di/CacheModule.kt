package com.example.submission5setengah.core.di

import android.content.Context
import dagger.Module
import dagger.Provides
import javax.inject.Singleton

//@Module
class CacheModule {

   /* @Singleton
    @Provides
    fun provideCache(context: Context): DataStore<Preferences> = context.applicationContext.dataStore

    @Provides
    fun provideStore(store: DataStore<Preferences>) = SettingStore(store)*/

}